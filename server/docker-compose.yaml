services:
    api:
        build: .
        env_file:
            - .env
        ports:
            - "${HOST_PORT:-5000}:5000"
        volumes:
            - .:/app
            - db_data:/data
        command: flask --app app run --debug --host=0.0.0.0 --port=5000

volumes:
    db_data:
